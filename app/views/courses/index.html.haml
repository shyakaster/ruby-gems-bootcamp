%h1 Listing courses

We are proud owners of Courses
= @courses.count
%p
.card-columns
  - @courses.each do |course|
    = render 'courses/course', course: course
%br
= link_to 'New Course', new_course_path, class: 'btn btn-big btn-primary'
